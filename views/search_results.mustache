<div class="container">
  <header class="page-header">
    <h1>Suchergebnisse <small>für '{{ query }}'</small></h1>
  </header>

  <ul class="onebox-results">
    {{#results}}
      <li class="list-group-item selected">
        <a href="/partners/{{ partnerId }}">
            <h3>
                <span class="p-honorific-prefix">{{ honorific }}</span>
                <span class="p-name">{{ name }}</span>
                <!-- TODO: add branch indicators -->
            </h3>
            <p>
                <span class="p-post-code">{{ address }}</span>
            </p>
            <p>{{ age }} Jahre, geb. am <span class="dt-bday">{{ dob }}</span></p>
        </a>
      </li>
    {{/results}}
  </ul>

</div>
